{
  "type": "module",
  "devDependencies": {
    "terser": "^5.50.0",
    "vite": "^8.2.1",
    "wrangler": "^4.125.0"
  },
  "scripts": {
    "postinstall": "git config core.hooksPath .githooks",
    "dev": "vite",
    "deploy": "make && wrangler pages deploy dist"
  }
}
